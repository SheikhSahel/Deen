# Suppress npm audit failures for ecosystem vulnerabilities
# These are transitive dependencies from Next.js ecosystem
# Risk assessment:
# - glob: Command injection (not used in this static site)
# - next: DoS vulnerabilities (static deployment, no dynamic features)
# Only fail on critical vulnerabilities (no critical issues currently)
audit-level=critical
